package com.minus.git.reactive.backend

//import com.minus.git.server.http.git.gitDaemon.GitServer
//import org.springframework.boot.context.event.ApplicationReadyEvent
//import org.springframework.context.ApplicationListener
//import java.util.logging.Logger

//@Component
class BootGitServer/* : ApplicationListener<ApplicationReadyEvent> */{
//    private val logger = Logger.getLogger(BootGitServer::class.toString())
//    @Autowired
//    private lateinit var gitServer: GitServer

//    @EventListener
//    override fun onApplicationEvent(event: ApplicationReadyEvent) {
//        if (gitServer.isRunning) {
//            logger.info("Server is already running")
//            return
//        }
//
//        logger.info("Started git server")
//        gitServer.start()
//    }
}